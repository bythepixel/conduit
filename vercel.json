{
    "crons": [
        {
            "path": "/api/cron-all",
            "schedule": "0 4 * * *"
        }
    ],
    "buildCommand": "npx prisma generate && npm run build",
    "installCommand": "npm install"
}
